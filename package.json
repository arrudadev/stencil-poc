{
  "name": "stencil-poc",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --filter=./packages/*"
  },
  "devDependencies": {
    "turbo": "^1.7.0"
  }
}
